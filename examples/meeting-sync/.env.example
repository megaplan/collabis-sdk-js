# OAuth 2.1 bearer access token (audience = https://api.collabis.ru)
COLLABIS_TOKEN=

# A page id to receive the meeting subpage / hold the Meetings database
COLLABIS_PARENT_PAGE_ID=

# Optional: override the API origin for a non-prod environment
# COLLABIS_BASE_URL=

# Optional (sync-database.ts): reuse an existing database instead of creating one
# COLLABIS_DATABASE_ID=
